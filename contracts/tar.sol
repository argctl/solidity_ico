pragma solidity >= "0.8.20";
import "./argctl.sol";
import "./ltcgra.sol";
import "./C.sol";

contract tar {
 
  constructor (address _handshakes, address gitorg_, address gitarg_, address gitarray_) {
    //new tar(_handshakes, gitorg_, gitarg_, gitarray_);
    //new ltcgra(_handshakes, gitorg_, gitarg_, gitarray_);
    //new C(_handshakes, gitorg_, gitarg_, gitarray_);
    //new C(_handshakes, gitorg_, gitarg_);

    //new C(_handshakes, gitorg_);
  }
}
